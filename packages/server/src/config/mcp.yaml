# MCP servers available to agents.
#
# Auth headers are configured via env vars, never in this file:
#   MCP_HEADERS         - default headers for every MCP server
#   MCP_{NAME}_HEADERS  - per-server overrides ({NAME} = server name,
#                         uppercased, non-alphanumerics replaced by "_")
mcp_servers:
  - name: github-remote
    url: https://internal.devtest.truefoundry.tech/api/llm/truefoundry/mcp/github-remote/server
